#lanche = ('Hambúrguer', 'Suco', 'Pizza', 'Pudim', 'Batata Frita')

##Enumerate retorna 2 valores, a posição e o item, varridos de dentro de uma tupla/lista/dicio
#for pos, comida in enumerate(lanche):
#    print(f'Eu vou comer {comida} na posição {pos}')
#print('\n')
#print(sorted(lanche)) ## ordena a tupla

#a = (2, 5, 4)
#b = (5, 8, 1, 2)
#c = b+a
#print(c.count(5)) ## comando count conta quantas vezes o 5 aparece dento da tupla c
#print(c.index(8)) ## index retorna em que posição está o número 8
#print(c.index(5, 1)) ## index com deslocamento. Retorna onde está o 5 dentro da tupla, começando a varredura a partir da pos 1



